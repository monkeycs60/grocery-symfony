{% extends 'admin/admin_base.html.twig' %}

{% block title %}Administration - Utilisateurs
{% endblock %}

{% block admincontent %}
	<div class="w-100 h-100 d-flex flex-column gap-0 pt-16 text-center px-12">
		<p class="large-font-size fw-bold mb-6">
			Bienvenue sur le pannel d'administration des utilisateurs de Culinaria.
		</p>
		<div class="table-responsive">
			<table class="table">
				<thead>
					<tr>
						<th>Nom d'utilisateur</th>
						<th>Email</th>
						<th>Adresse</th>
						<th>Préférence de livraison</th>
					</tr>
				</thead>
				<tbody>
					{% for user in users %}
						<tr>
							<td>
								<a href="{{ path('app_admin_users_details', {'id': user.id}) }}">{{ user.username|default(user.email) }}</a>
							</td>
							<td>{{ user.email }}</td>
							<td>{{ user.address }}</td>
							<td>{{ user.deliveryPreference }}</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</div>
{% endblock %}
